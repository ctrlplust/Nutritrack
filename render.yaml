services:
  - type: web
    name: nutritrack-api
    runtime: node
    repo: https://github.com/ctrlplust/Nutritrack
    plan: free
    buildCommand: pnpm run build
    startCommand: cd artifacts/api-server && node --enable-source-maps ./dist/index.mjs
    envVars:
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        sync: false
      - key: FATSECRET_CLIENT_ID
        sync: false
      - key: FATSECRET_CLIENT_SECRET
        sync: false
